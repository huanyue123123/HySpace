<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huanyue.spacepageclient.dao.orcl.TestUserDao">

    <select id="getTestUser" resultType="java.util.HashMap">
        SELECT
	        *
        FROM
	        KA
        WHERE
	        KA."status" = '1'

    </select>
</mapper>
